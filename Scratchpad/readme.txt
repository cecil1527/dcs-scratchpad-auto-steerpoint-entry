# some general notes: you can do comments by starting a line with #

# it expects the word Steerpoint to mark the start of the steerpoint info and elevation info to mark the end.

# also, it's useful to have a separate file per airframe, since steerpoints are formatted slightly differently per airframe

# only the F-16, F-18, and AH-64 are supported for now